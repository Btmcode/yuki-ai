# TikTokLive (TikTok Live API wrapper)
TikTokLive==7.0.0

# Google Gemini — yeni SDK (TTS + LLM)
google-genai>=1.0.0

# Eski Gemini SDK (geri uyumluluk için — kullanımdan kalktı)
google-generativeai==0.8.3

# ElevenLabs (opsiyonel — premium TTS, ücretsiz tier 10k char/ay)
elevenlabs==1.4.1

# Edge-TTS (Microsoft Neural TTS — çok akıcı Türkçe, ücretsiz)
# Bu en iyi fallback — gTTS'ten çok daha doğal
edge-tts>=7.0.0

# gTTS (son çare TTS — her zaman çalışır ama robotik)
gTTS==2.2.4

# OBS WebSocket
obsws-python==5.0.0

# Socket.IO client (bridge ile iletişim)
python-socketio[client]==5.11.0

# Dotenv (.env dosyası okuma)
python-dotenv==1.0.1

# HTTP
requests==2.32.3

# Audio (TTS çıktısını çalmak için — sadece gerçek makinede gerekli)
# pyaudio==0.2.14  # sandbox'ta gerek yok, lokal makinede aç
